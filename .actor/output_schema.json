{
	"actorOutputSchemaVersion": 1,
	"title": "CZ/SK Company Info — output",
	"properties": {
		"dataset": {
			"type": "string",
			"title": "Company dataset",
			"description": "One row per IČO (or two rows when an auto-mode IČO collision exists in both registries).",
			"template": "{{links.apiDefaultDatasetUrl}}/items?clean=true"
		}
	}
}
